@model TARge21Shop.Models.RealEstate.FileToApiViewModel

@{
    var photoPaths = "~/multipleFileUpload/" + (Model.FilePath ?? "noimage.jpg");
}

<div class="form-group row col-sm-4 offset-2">
    <div>@Model.FilePath</div>
    <img class="card-img-top" src="@photoPaths" asp-append-version="true" />
    @*<input asp-action="removeImage" asp-controller="spaceships" asp-route-imageId="@Model.ImageId" type="submit" class="btn btn-danger" value="Remove Image" />*@
</div>